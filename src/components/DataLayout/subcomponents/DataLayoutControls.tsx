import { styled } from "@mui/material"

const DataLayoutControls = styled("div")({
  display: "flex",
  alignItems: "center",
  columnGap: "8px",
})

DataLayoutControls.displayName = "DataLayoutControls"

export { DataLayoutControls }
